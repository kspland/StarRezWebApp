@using RL.Models;

@model   IEnumerable<AppGender>

@{
    ViewBag.Title = "AddAppGender";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AddAppGender</h2>

<body>
    <div>
        <p>This program  adds the gender to the Entry Application via a CSV file. </p>
        <p>An example of how the CSV file is listed below. </p>
        <p><Bold>The Column names must be named exactly like the example and in the same order.</Bold></p>
    </div>

    <div>
        <table class="table table-bordered table-hover">
            <tr>
                <th>EntryApplicationID</th>
                <th>EntryID</th>
                <th>Gender</th>
                <th>Classification</th>
                <th>Status</th>

            </tr>
            <tr>
                <td>11129</td>
                <td>60234</td>
                <td>M</td>
                <td>New</td>
                <td></td>
            </tr>
            <tr>
                <td>95345</td>
                <td>56789</td>
                <td>F</td>
                <td>Continuing</td>
                <td></td>
            </tr>
            <tr>
                <td>94921</td>
                <td>1575</td>
                <td>M</td>
                <td>Continuing</td>
                <td></td>
            </tr>
            <tr>
                <td>164098</td>
                <td>65444</td>
                <td>F</td>
                <td>New</td>
                <td></td>
            </tr>
        </table>
    </div>

    <div>
        <hr />
        <br />
        @using (Html.BeginForm("AddAppGender", "IS", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <input type="file" name="postedFile" />
            <input type="submit" value="Import" />
            <p>Enter your PawsID <input type="text" name="username" /></p>
            <p>Enter your Password<input type="password" name="password" /></p>
        }
    </div>

    <div>
        @if (Model.Count() > 0)
        {
            <div>
                <table class="table table-hover table-bordered">
                    <tr>
                        <th>EntryApplcationID</th>
                        <th>EntryID</th>
                        <th>Gender</th>
                        <th>Classification</th>
                        <th>Status</th>
                    </tr>
                  
                        @foreach( AppGender A in Model)
                        {
                            <tr>
                                <td>@A.EntryApplicationID</td>
                                <td>@A.EntryID</td>
                                <td>@A.Gender</td>
                                <td>@A.Classification</td>
                                <td>@A.Status</td>
                            </tr>
                        }
                   
                </table>
            </div>
        }
    </div>
</body>